# Ralph Progress Log

## Codebase Patterns

### How to query episode_summaries.jsonl
NEVER read the file directly (2.3MB). Always use `python3 -c` via Bash:
```python
import json
lines = open('artifacts/episode_summaries.jsonl').readlines()
eps = [json.loads(l) for l in lines]
# then aggregate
```

### Episode summary structure
Keys: altitude_at_apogee_m, altitude_at_burnout_m, altitude_ratio_to_target,
curriculum_target_altitude_m, episode, fuel_used_fraction, gimbal_variance,
max_altitude_m, max_g_load, max_q_dyn, mean_throttle, reward_components,
success, t, termination_reason, throttle_variance, time_to_apogee_s

reward_components keys: coasting_pen, fuel_pen, g_pen, omega_pen, progress,
q_pen, smooth_pen, survival, terminal_bonus, terminal_penalty, tilt_pen

### How reward is computed in env.py
- step(): computes per-step reward components, accumulates into self.reward_sums
- terminal reward: added at episode end (success +100, failure -(50+25*err))
- info_dict() populates episode summary via train.py callback
- _progress_potential(): phi = clip(alt/target, 0, 2) — pure altitude shaping

### How telemetry fields get into episode_summaries.jsonl
1. env.py info_dict() returns a dict with all episode stats
2. train.py callback reads info["reward_components_cumulative"] at episode end
3. Writes a JSON line to artifacts/episode_summaries.jsonl with flattened fields
4. To ADD a new field: add it to info_dict() return dict in env.py, then ensure train.py callback writes it

### Curriculum target
- Set via env.set_curriculum(target_altitude_m) in train.py CurriculumManager
- Stored as self.curriculum_target_altitude_m (float, meters)
- Only changes when success_threshold met over rolling window
- phi = alt / curriculum_target_altitude_m (not mission.target_altitude_m)

---

## 2026-03-25 - S1: Diagnose Run 12 and write ANALYSIS.md

### What was done
- Created ralph/run12-analysis-and-fixes branch from main
- Analyzed 2510 episodes from artifacts/episode_summaries.jsonl via Python scripts
- Wrote ANALYSIS.md at project root with:
  - q compliance by 5 altitude bands (0-40, 40-60, 60-80, 80-100, 100+ km)
  - Reward-vs-altitude table confirming monotonicity but near-flat gradient below target
  - All reward component averages showing terminal_penalty = -58.4 (80% of negative signal)
  - Success rate rolling windows (3-6.5%, no improving trend, flat from ep 1 to ep 2510)
  - Burnout altitude stats (mean 24.1km ± 1.5km)
  - 3 ranked hypotheses: H1 terminal penalty dominance (9/10), H3 throttle suppression (8/10), H2 flat progress gradient (7/10)
- Updated prd.json S1 passes: true
- Wrote SCRATCHPAD.md in scripts/ralph/

### Reasoning
- S1 is pure analysis — no code changes, creates evidence base for S2-S5
- Key finding: curriculum target was fixed at 100km the entire run (never promoted) — policy was always trying to reach 100km but stuck at 86km
- Key finding: reward is monotonically increasing with altitude but gradient is near-flat below target (0.35/km), then jumps +103 at 100km threshold
- Key finding: terminal_penalty avg -58.4 dominates 80% of negative gradient → shaping signals can't pull policy off 86km plateau

### Risks noted
- q compliance is inferred from max_q_dyn (worst point per episode), not per-step compliance rate
- Throttle analysis is at episode level; per-phase throttle (powered vs coast) would be more precise — S2 will add burnout velocity to clarify

### Learnings for future iterations
- Curriculum target was 100km throughout Run 12 (not 90km as mentioned in STATUS.md "80→92km+") — STATUS.md was written during early Run 12 and not updated at completion
- The warm-start from Run 11 placed policy immediately at ~87km avg from episode 1 — policy never explored lower altitudes in Run 12
- tilt_pen = -3.65 is the second largest penalty after terminal_penalty — worth monitoring if it reflects meaningful flight instability or just gimbal use
- 99.7% of episodes terminate at apogee (correct) — only 0.3% crash (good sign)

### Confidence
- Implementation correct: 9/10
- Hypothesis valid (analysis accurately reflects dynamics): 9/10

---

## 2026-03-25 - S2: Measure and log velocity at burnout per episode

### What was done
- Added `velocity_at_burnout_mps` (total speed) and `vz_at_burnout_mps` (vertical component) to `env.py info_dict()`
- Both derived from `self.burnout_scalars.speed` and `.vz` (same pattern as `altitude_at_burnout_m`)
- Added comment: "vz_at_burnout determines apogee via vz²/2g"
- Added both fields to `train.py _on_step()` row dict so they appear in episode_summaries.jsonl

### Reasoning
- Burnout at 24.1km avg; to coast to 100km needs vz_burnout >= sqrt(2*9.81*76,000) ~= 1,220 m/s
- Policy reaches ~87km avg -> implies vz_burnout ~= 1,100 m/s (90% of needed)
- Next run JSONL will show if failed episodes are velocity-limited (vz<1,100) or trajectory-limited
- Zero-risk observability change -- no reward logic touched

### Risks noted
- None -- pure measurement. Fields show as MISSING in existing JSONL (expected, appears in next run)

### Learnings for future iterations
- burnout_scalars (EpisodeScalars) already has all velocity components (.vz, .speed, .vx, .vy)
- Pattern for adding telemetry: (1) extract from self.burnout_scalars in info_dict(), (2) add to train.py row dict
- env.py's info_dict() is already very long -- scan it carefully before adding to avoid duplicate names

### Confidence
- Implementation correct: 9/10
- Hypothesis valid (measurement is useful): 10/10

---

## 2026-03-25 - S3: Reduce terminal penalty coefficient to free up reward gradient

### What was done
- Changed `fail_pen = 50.0 + 25.0 * terminal_penalty` to `fail_pen = 25.0 + 10.0 * terminal_penalty` in env.py
- Added comment explaining the reasoning (was 67% of gradient, now ~48%)
- No other reward terms touched; success bonus (+100) and stall_pen (25.0) unchanged

### Reasoning
- Run 12 data: terminal_penalty avg -58.4, non-terminal sum +14.1 → penalty was 80% of negative signal
- At avg err_cost=0.336: old fail_pen = 50+25*0.336 = 58.4; new = 25+10*0.336 = 28.4
- Ratio of terminal-to-shaping drops from ~4:1 to ~2:1, allowing progress/q signals to matter more
- Specific: at 86km miss (err=0.14 alt fraction, stage="high", total err_cost ~0.34):
  old penalty = -(50+25*0.34) = -58.5; new = -(25+10*0.34) = -28.4 (52% reduction)
- Falsification: if avg altitude drops (policy stops caring about reaching target) or success stays flat

### Risks noted
- Both S3 and S4 act in the same direction (amplify shaping signals) — they are being batched into Run 13
  together since the PRD says S4 should only be added after S3 "validated or user decides to batch"
- The stall_pen (25.0 extra for very early failures) is unchanged — maintains a strong floor penalty

### Confidence
- Implementation correct: 10/10
- Hypothesis valid: 9/10

---

## 2026-03-25 - S4: Reshape progress potential to have steeper gradient in 80-100km band

### What was done
- Changed _progress_potential() in env.py from:
    phi = clip(alt/target, 0, 2)   [pure linear]
  to:
    base = clip(alt/target, 0, 1)
    bonus = 0.5 * clip((alt - 0.8*target) / (0.2*target), 0, 1)
    phi = base + bonus
- Below 80% of target altitude: behavior unchanged (early curriculum stages unaffected)
- Phi values: 50km→0.5, 80km→0.8, 85km→0.975, 90km→1.15, 95km→1.325, 100km→1.5
- Marginal progress reward for 86km→100km: old 0.56, new 1.96 (3.5x stronger)

### Reasoning
- Run 12: progress reward only +3.0 at <80km vs +4.2 at >100km — only +1.2 difference over the full altitude range
- Gradient in 80-100km band was 4*(1.0-0.86)/episode = 0.56, nearly same as linear everywhere else
- New formula makes each km in 80-100km band worth 2.5x a km below 80km
- Batched with S3 into Run 13 as PRD allows batching when user decides

### Risks noted
- Batching S3+S4 in same run: both act in same direction. If they combine to overshoot (policy ignores q in final push), we won't know which one to roll back. Acceptable risk for Run 13.
- phi above 100km is capped at 1.5 (was 2.0 at 200km in old formula) — slight reduction in incentive for >100km overshoot, which is fine (we want exactly 100km, not more)

### Confidence
- Implementation correct: 10/10 (verified numerically: phi at 0/50/80/85/90/95/100km all correct)
- Hypothesis valid: 7/10 (gradient flattening was contributing, but terminal penalty reduction is the primary fix)

---
